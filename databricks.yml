# This is a Databricks asset bundle definition for test_databricks_asset_bundles.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test_databricks_asset_bundles
  uuid: da3439c8-68ba-48bd-bcaa-af8f4ee4ad34

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-8426789340308003.3.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-8426789340308003.3.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/daniel.iyiola@godevsuite072.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/daniel.iyiola@godevsuite072.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: daniel.iyiola@godevsuite072.onmicrosoft.com
        level: CAN_MANAGE
